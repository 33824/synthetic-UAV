# Temporary data-transfer branch (DUT Anti-UAV -> YOLO format)
This orphan branch exists only to move dataset chunks to a GPU server.
It will be deleted after transfer. Reassemble: cat chunks/part_a_* > drone_yolo.tar
